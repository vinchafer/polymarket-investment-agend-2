# Public URL of your FastAPI dashboard on the VPS (scheme + host + port), no trailing slash path
NEXT_PUBLIC_API_BASE=https://your-subdomain.yourdomain.tld:8765

# Must match DASHBOARD_READ_TOKEN on the VPS if you enabled token auth
NEXT_PUBLIC_DASHBOARD_TOKEN=
